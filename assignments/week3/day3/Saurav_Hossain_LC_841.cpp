class Solution 
{
public:
    bool canVisitAllRooms(vector<vector<int>>& rooms) 
    {
        //so we want to go through each room and return true if we can go to all rooms
        
        //my understanding is that we would do something like go into the first possible room then get all the keys
        //go to all the key rooms then get all those keys
        
        //what if you start in a room that gives us no keys
            //return false
    
        //i guess keep track of all visited rooms
        //would need a seperate stored space to keep track of visited since we can double back if we need so this isnt just find the smallest path or any path
        
    }
};
