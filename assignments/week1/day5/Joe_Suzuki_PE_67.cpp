#include <bits/stdc++.h>
using namespace std;

int sieve_largest_pf(int num){
	int s = sqrt(num);
	return 0;
}

int main(int argc, char **argv)
{
	ios::sync_with_stdio(0);
	cin.tie(0);
	
	freopen("input.txt", "r", stdin);
	vector<int> v = {stdin};
	return 0;
}

